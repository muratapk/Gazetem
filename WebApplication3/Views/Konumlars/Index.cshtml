@model IEnumerable<WebApplication3.Models.Konumlar>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KonumAdi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KonumAdi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KonumId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KonumId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KonumId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
